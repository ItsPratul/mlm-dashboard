<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />

    <title>MLM | Admin Login</title>

    <!-- Favicon -->
    <link href="assets/img/favicon.png" rel="icon" />

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Bootstrap -->
    <link
      href="assets/vendor/bootstrap/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <!-- Bootstrap Icons -->
    <link
      href="assets/vendor/bootstrap-icons/bootstrap-icons.css"
      rel="stylesheet"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        height: 100vh;
        overflow: hidden;
        font-family: "Poppins", sans-serif;
        background: linear-gradient(135deg, #0f172a, #1e293b);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 25px;
      }

      .main-box {
        width: 100%;
        max-width: 920px;
      }

      .brand {
        text-align: center;
        margin-bottom: 18px;
      }

      .brand h1 {
        font-size: 40px;
        font-weight: 700;
        margin-bottom: 6px;
        color: #ffffff;
      }

      .brand span {
        color: #60a5fa;
      }

      .brand p {
        color: rgba(255, 255, 255, 0.75);
        font-size: 13px;
      }

      .login-card {
        background: #ffffff;
        border-radius: 24px;
        overflow: hidden;
        display: flex;
        min-height: 480px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.25);
      }

      .left-side {
        width: 42%;
        background: linear-gradient(135deg, #2563eb, #1d4ed8);
        color: white;
        padding: 38px 30px;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .left-side h2 {
        font-size: 28px;
        font-weight: 700;
        margin-bottom: 14px;
        line-height: 1.3;
      }

      .left-side p {
        color: rgba(255, 255, 255, 0.88);
        line-height: 1.7;
        font-size: 13px;
      }

      .feature-box {
        margin-top: 24px;
      }

      .feature-item {
        display: flex;
        align-items: center;
        margin-bottom: 14px;
        font-size: 13px;
      }

      .feature-item i {
        margin-right: 10px;
        font-size: 16px;
      }

      .right-side {
        width: 58%;
        padding: 38px 35px;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .login-title {
        font-size: 30px;
        font-weight: 700;
        color: #111827;
        margin-bottom: 8px;
      }

      .login-subtitle {
        color: #6b7280;
        margin-bottom: 24px;
        font-size: 13px;
      }

      .form-label {
        font-size: 13px;
        font-weight: 500;
        color: #374151;
        margin-bottom: 8px;
      }

      .input-group {
        margin-bottom: 16px;
      }

      .input-group-text {
        border-radius: 12px 0 0 12px;
        background: #f1f5f9;
        border: 1px solid #d1d5db;
        padding: 0 14px;
      }

      .form-control {
        height: 50px;
        border-radius: 0 12px 12px 0;
        border: 1px solid #d1d5db;
        box-shadow: none !important;
        font-size: 14px;
      }

      .form-control:focus {
        border-color: #2563eb;
      }

      .login-btn {
        width: 100%;
        height: 50px;
        border: none;
        border-radius: 12px;
        background: linear-gradient(to right, #2563eb, #1d4ed8);
        color: white;
        font-size: 15px;
        font-weight: 600;
        transition: 0.3s;
        margin-top: 6px;
      }

      .login-btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 8px 20px rgba(37, 99, 235, 0.35);
      }

      .demo-box {
        margin-top: 18px;
        background: #f8fafc;
        border: 1px dashed #cbd5e1;
        border-radius: 14px;
        padding: 14px;
        text-align: center;
      }

      .demo-box h6 {
        font-weight: 700;
        margin-bottom: 8px;
        color: #2563eb;
        font-size: 15px;
      }

      .demo-box p {
        margin: 3px 0;
        font-size: 12px;
        color: #4b5563;
      }

      .footer-text {
        margin-top: 14px;
        text-align: center;
        color: rgba(255, 255, 255, 0.75);
        font-size: 12px;
      }

      @media (max-width: 850px) {
        body {
          height: auto;
          overflow-y: auto;
          padding: 18px;
        }

        .login-card {
          flex-direction: column;
        }

        .left-side,
        .right-side {
          width: 100%;
        }

        .left-side {
          padding: 30px 24px;
        }

        .right-side {
          padding: 30px 24px;
        }

        .brand h1 {
          font-size: 34px;
        }

        .login-title {
          font-size: 26px;
        }
      }
    </style>
  </head>

  <body>
    <%- include("../../utils/flash"); %>

    <div class="main-box">
      <div class="brand">
        <h1>M<span>LM</span></h1>

        <p>Secure Referral & Wallet Management Dashboard</p>
      </div>

      <div class="login-card">
        <div class="left-side">
          <h2>Manage Your Network Efficiently</h2>

          <p>
            Access referrals, commissions, wallet transactions, and analytics
            through a secure and modern dashboard system.
          </p>

          <div class="feature-box">
            <div class="feature-item">
              <i class="bi bi-check-circle-fill"></i>
              Referral Tracking
            </div>

            <div class="feature-item">
              <i class="bi bi-wallet2"></i>
              Wallet Monitoring
            </div>

            <div class="feature-item">
              <i class="bi bi-bar-chart-fill"></i>
              Dashboard Analytics
            </div>
          </div>
        </div>

        <div class="right-side">
          <h2 class="login-title">Welcome Back!</h2>

          <p class="login-subtitle">Sign in to continue to your dashboard</p>

          <form action="/user_login_save" method="post">
            <div>
              <label class="form-label">Email Address</label>

              <div class="input-group">
                <span class="input-group-text">
                  <i class="bi bi-envelope-fill"></i>
                </span>

                <input
                  type="email"
                  name="email"
                  class="form-control"
                  placeholder="Enter your email address"
                  required
                />
              </div>
            </div>

            <div>
              <label class="form-label">Password</label>

              <div class="input-group">
                <span class="input-group-text">
                  <i class="bi bi-lock-fill"></i>
                </span>

                <input
                  type="password"
                  name="password"
                  class="form-control"
                  placeholder="Enter your password"
                  required
                />
              </div>
            </div>

            <button class="login-btn" type="submit">
              <i class="bi bi-box-arrow-in-right"></i>
              Sign In
            </button>
          </form>

          <div class="demo-box">
            <h6>Demo Access</h6>

            <p>Email: admin@gmail.com</p>

            <p>Password: Available on request</p>
          </div>
        </div>
      </div>

      <p class="footer-text">© 2026 Pratul Khandelwal. All rights reserved.</p>
    </div>

    <script src="assets/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
